#include <stdio.h>

int main()
{
    int a = 15;
    int b = 5;
    int c = b + a ;
//=+ arethmatic operators, b and a is operants and c is result.
             
    printf("the value of a is %d and b is %d and sum is %d\n",a,b,c);  
    printf("the subtract a is %d and b is %d\n ",a-b);

// modulus operaters used  to get the remainder.

           printf ("the remainder when a is divaided by b is %d \n " ,a%b );
           printf("the value of a*b is %d ",a*b);
           
    return 0;
}